# 個人 AI 助手專案

## 這是什麼
邱彥碩的個人 AI 助手專案。單一專案，涵蓋兩塊互相依賴的功能：**長期記憶系統**與**晨報自動化系統**。兩者合併在同一個專案裡，因為晨報需要讀取記憶系統維護的使用者背景資料。

## 專案目標

### 1. 長期記憶
- 匯入過去在 Gemini 上的對話紀錄，了解使用者過去的對話習慣、關注的主題、個性與偏好
- 建立一份會持續演化的核心背景摘要，之後隨著使用者在 Claude 上的互動，慢慢補充、更新這份理解
- 敏感或原始個資（例如完整對話原文）不應該被直接保存或引用，只保留摘要層級的理解

### 2. 晨報系統（全部重建，非沿用舊版）
使用者原本有一套晨報系統（Python + Claude Haiku + GitHub Actions + Telegram Bot，部署於 GitHub Pages），但功能不完整，**這次要整個重新設計、重新實作**，不是在舊版上修補。

重建範圍包含：
- **資料來源**：讀取 `memory/` 裡的長期記憶（使用者背景、自我提升進度追蹤），未來會加入 Discord（使用者自己專屬的私人伺服器，自己手動記錄事項）作為額外來源
- **內容邏輯**：今日行程 + 延續追蹤先前晨報提過的自我提升內容（不是每天從零開始），並且能依使用者回饋動態調整內容與風格
- **推播管道**：改用 LINE（取代原本的 Telegram），透過 LINE Messaging API 的 Push Message 推到使用者手機
- **排程**：延續使用 GitHub Actions 做每日排程觸發
- **視覺風格**：**沿用「發車看板 / 交通動態顯示牌」的美學風格**，這是使用者喜歡且想保留的設計語言，靜態網頁部分繼續部署在 GitHub Pages

### 3. 使用者背景
- 台大資工二年級，正在建立實作經驗，熟悉 Python 與基礎演算法
- 常在台北大安區與台中之間往返
- 對睡眠優化、時間管理、減少資訊過載等自我提升主題有興趣

## 給 Claude Code 的執行原則
- 資料夾裡目前已經有從 Google Takeout 匯出的 Gemini 對話紀錄，請自行判斷檔案結構、決定合適的資料夾規劃與處理方式，不需要照特定格式，用你認為最合理的方式組織即可
- 原始匯出檔（可能含個資）與任何 API token / secret 不應該被 commit 進 git，請自行建立適當的 .gitignore
- 晨報系統是**全部重建**，可以參考舊版邏輯與視覺風格作為靈感，但程式碼與架構請重新設計，不要假設要相容舊版 repo 結構
- 處理過程中，只有遇到「需要使用者去外部網站操作」（例如申請 LINE Messaging API channel、之後的 Discord Bot 申請）才需要停下來詢問，其餘判斷與實作請自主完成
- 建議的實作順序：先完成記憶系統的基礎（Gemini 資料處理、profile.md），再重建晨報系統框架（先不含 Discord，用既有記憶資料先跑通），Discord 串接留待使用者之後另外確認再加入
- 每完成一個階段，跟使用者回報做了什麼、產出了哪些檔案，並說明下一步建議
